{:itonami.blueprint/id "cloud-itonami-iso3166-cog"
 :itonami.blueprint/name "Independent Public-Sector Market-Entry & Procurement Compliance Service — Republic of the Congo"
 :itonami.blueprint/iso3166 "COG"
 :itonami.blueprint/domain :public-sector/market-entry-compliance
 :itonami.blueprint/license "AGPL-3.0-or-later"
 :itonami.blueprint/governor :market-entry-compliance-governor
 :itonami.blueprint/robotics false
 :itonami.blueprint/required-technologies [:identity :forms :dmn :bpmn :audit-ledger]
 :itonami.blueprint/status :public-oss
 :itonami.blueprint/maturity :implemented
 :itonami.blueprint/implemented-slice
 "MarketEntry-LLM ⊣ Market-Entry Compliance Governor, langgraph-clj
 StateGraph, append-only audit ledger. Seven governor checks grounded
 in primary Republic of the Congo sources (curl/pdftotext-verified
 against sgg.cg's own hosting of Décret n° 2009-156 du 20 mai 2009
 portant Code des Marchés Publics, and WebFetch-verified against
 armp.cg / acpce.cg / impots.gouv.cg): spec-basis citation, evidence-
 checklist completeness (RCCM via ACPCE + NIU via DGID + ARMP
 exclusion-list confirmation + BOAMP/ARMP registration), a flagship
 STATUTORY CEILING VALIDATION on an engagement's own declared prior-
 ARMP-exclusion duration against Art. 146's five-year statutory
 maximum (with the court-ordered-definitive-exclusion exception Art.
 146 itself names), engagement-fee ground-truth recompute, NIU
 registration verification, and a hydrocarbons-SECTOR-CONDITIONAL
 local-content/SNPC-joint-venture check (15 November 2019 executive
 order -- petroleum only, never applied to non-hydrocarbons
 engagements). `:filing/draft`/`:filing/submit` NEVER auto-commit at
 any phase (permanent structural invariant); `:engagement/intake` is
 the only op eligible to auto-commit, and only at phase 3 when
 governor-clean."
 ;; Deployed into the ai-gftd-repository-dispatch namespace (Workers for
 ;; Platforms). A user Worker there has no URL of its own — it is reachable
 ;; only through the dispatch Worker, so the address is the router plus this
 ;; actor's repository name, which is also the catalog's :repo key.
 ;; /health answers JSON, so it is declared as the probe path.
 :itonami.blueprint/endpoint "https://itonami-fleet-dispatch.04-feasts-minded.workers.dev/cloud-itonami-iso3166-cog"
 :itonami.blueprint/endpoint-kind :dispatch-namespace
 :itonami.blueprint/health-path "/health"}
